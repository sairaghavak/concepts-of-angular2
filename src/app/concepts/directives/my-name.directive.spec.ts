import { MyNameDirective } from './my-name.directive';

describe('MyNameDirective', () => {
  it('should create an instance', () => {
    const directive = new MyNameDirective();
    expect(directive).toBeTruthy();
  });
});
